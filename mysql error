i changed the mysqli line to mysql in the index.php file

<?php
//phpinfo();
//echo $_SERVER['HTTP_USER_AGENT'];

$servername = "localhost";
$username = "";
$password = "";

// create connection
$conn = mysql_connect($servername, $username, $password);

// Check connection
if ($conn->connect_error) {
    die("Connection failed: " . $conn->connect_error);
}
?>

now i get this new error


Fatal error: Call to undefined function mysql_connect() in /www/index.php on line 10
